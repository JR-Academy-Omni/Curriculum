{
  "name": "skill-workshop-web",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "description": "JR Academy · Build Your First Claude Skill — 网页版课程 PPT",
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export": "slidev export",
    "export:pdf": "slidev export --output dist/skill-workshop.pdf",
    "comment": "PDF export 需要单独装 playwright: npm i -D playwright-chromium && npx playwright install chromium"
  },
  "dependencies": {
    "@slidev/cli": "^0.50.0",
    "@slidev/theme-default": "^0.25.0",
    "vue": "^3.5.0"
  }
}
